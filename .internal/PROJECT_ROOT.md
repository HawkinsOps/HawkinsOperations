# Project Root Contract

Canonical local root: `C:\GitHub\HawkinsOperations`

All future prompts, scripts, setup notes, and documentation in this repository should assume this local root unless explicitly overridden for a specific environment.

Path guidance:
- Prefer repo-relative paths (for example, `scripts/`, `docs/`, `site/`) in scripts and docs when absolute paths are not required.
- Use the canonical absolute root only when an absolute local path is explicitly needed.
- Do not change public URLs to reflect local filesystem paths.

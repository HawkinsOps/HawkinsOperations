Source path (for pipeline screenshot reference):
  PROOF_PACK/ARCHITECTURE.md → section: '## Deployment Architecture (Wazuh Example)'
  docs/execution/AUTOSOC_OPERATIONS_RUNBOOK_03-02-2026.md → section: '## Pipeline Components'

Rendered site location:
  output/playwright/autosoc-desktop.png (copied into this folder)
  This is a Playwright screenshot of the live site AutoSOC page.

Exact heading to capture from ARCHITECTURE.md:
  '## Deployment Architecture (Wazuh Example)'
  Contains 4 code blocks labeled: Development, Build Phase, Deployment, Verification

Screenshot recommendation:
  DO NOT screenshot the raw markdown code blocks — they will not render cleanly on LinkedIn.
  Re-create the 4 stages as a horizontal pipeline graphic in Canva or PowerPoint.
  Use autosoc-desktop.png as a background visual reference only.

For the AutoSOC pipeline flow, use these plain-English labels:
  Stage 1: Write detection rules (repo)
  Stage 2: Run build script (generates local_rules.xml)
  Stage 3: Deploy to Wazuh manager (scp to /var/ossec)
  Stage 4: Verify rule load (tail ossec.log)
